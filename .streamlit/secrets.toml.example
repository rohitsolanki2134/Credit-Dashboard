# ─────────────────────────────────────────────────────────────────────────────
# Streamlit Community Cloud — Secrets configuration
# ─────────────────────────────────────────────────────────────────────────────
# HOW TO USE:
#   1. Go to https://share.streamlit.io → your app → Settings → Secrets
#   2. Copy-paste the block below, filling in your real values
#   3. Click Save — the app restarts automatically
#
# DO NOT commit this file with real values. Keep secrets in the dashboard only.
# ─────────────────────────────────────────────────────────────────────────────

ANTHROPIC_API_KEY   = "sk-ant-api03-..."
LLM_PROVIDER        = "anthropic"
LLM_MODEL           = "claude-sonnet-4-5"
LLM_LITE_MODEL      = "claude-haiku-4-5"

SMTP_HOST           = "smtp.gmail.com"
SMTP_PORT           = "587"
SMTP_USER           = "btcinvoice@travelplusapp.com"
SMTP_PASSWORD       = "vdmuqcpnzraeabws"
SMTP_FROM           = "btcinvoice@travelplusapp.com"

DB_PATH             = "credit_dashboard.db"
